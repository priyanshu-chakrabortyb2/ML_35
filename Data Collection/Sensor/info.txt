Used a Random library to generate random data
activity = random.choice(activities) =>Select any activity from the specified list 
ax = round(random.uniform(0.8, 2.0), 2)=> generates no between 0.8,2.0 and round upto 2 decimal places
